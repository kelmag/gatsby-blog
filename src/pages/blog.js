import React from 'react';
import Layout from '../components/layout';

const BlogPage = () => {
    return (
        <Layout>
            <h1>Blog</h1>
            <div>
                This is where posts gonna be
            </div>
        </Layout>
    );
};

export default BlogPage;